import React from 'react';
import PropTypes from 'prop-types';

FileFeatures.propTypes = {
    
};

function FileFeatures(props) {
    return (
        <div>
            
        </div>
    );
}

export default FileFeatures;